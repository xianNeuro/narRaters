# ---------------------------------------------------------------------------
# Minimal install — everything below is REQUIRED to run the lightweight
# default ("minimal") method of every pipeline step:
#   Step 2 segment   --method clause       (heuristic, no ML deps)
#   Step 3 correct   --method rules        (pyspellchecker)
#   Step 4 parse     --method rules        (regex)
#   Step 5 match     --method keyword/test (keyword matcher, no ML deps)
#   Step 6 rate      --method linguistic   (regex; spaCy used only if present)
# plus the Flask web UI. `pip install -r requirements.txt` installs ONLY these
# and pulls no multi-GB ML frameworks — it will not fill up the user's disk.
# ---------------------------------------------------------------------------
flask>=2.3.0
pandas>=1.5.0
openpyxl>=3.0.0
pyspellchecker>=0.7.0
numpy>=1.24.0
matplotlib>=3.7.0
scipy>=1.10.0

# ---------------------------------------------------------------------------
# Optional extras — NOT installed by default. Each enables a heavier,
# non-minimal method. Install only the ones you need (or use the pyproject
# extras: `pip install narRaters[api]`, `[audio]`, `[nlp]`, `[match]`, ...).
# Local-model methods additionally run a free-disk-space preflight before any
# download (see helpers/disk_space.py) so they cannot crash the machine.
# ---------------------------------------------------------------------------

# API-based processing — Step 5 --method api, Step 6 --method api
# anthropic>=0.34.0
# openai>=1.0.0

# Audio transcription — Step 1 (pulls torch; multi-GB)
# openai-whisper>=20231117
# whisperx>=3.3.0

# Recall → story-event matching via PyPI rmatch — Step 5 --method rmatch.
# NOTE: rmatch transitively pulls torch + transformers + sentence-transformers
# + bitsandbytes (several GB). Install deliberately, not by default.
# https://github.com/gabrielkp/rmatch
# rmatch>=0.3.2

# NLP-enhanced clause detection — improves Step 2 --method fine/coarse accuracy
# spacy>=3.5.0
# benepar>=0.2.0
# To install: pip install spacy benepar && python -m spacy download en_core_web_sm

# Local Gemma spell/grammar via Transformers — Step 3 --method gemma (pulls torch)
# transformers>=4.44

# Additional grammar checking in spell-grammar-correct
# language-tool-python>=2.7.0

# Tutorial PDF generation (developer/ scripts, not published) — fpdf2>=2.7.0
